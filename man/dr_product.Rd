% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/products.R
\name{dr_product}
\alias{dr_product}
\title{Define a composable data product}
\usage{
dr_product(
  id,
  data = NULL,
  contract = NULL,
  version = "1.0.0",
  owner = NULL,
  description = NULL,
  code_version = NULL,
  source_name = NULL,
  execution = NULL,
  contracts = NULL,
  table = NULL
)
}
\arguments{
\item{id}{Product identity, unique within a dependency graph.}

\item{data}{Optional table, path, source adapter, product, or successful run.
Successful lake runs are pinned to their exact published release.}

\item{contract}{Optional contract, named type vector or prototype list.}

\item{version}{Optional immutable definition version. When omitted, lake
publication derives a technical version from the definition.}

\item{owner, description}{Optional metadata, otherwise inherited from contract.}

\item{code_version}{Optional code and dependency version. Required only when
explicitly reusing a previously published lake release with \code{cache = TRUE}.}

\item{source_name}{Optional name for \code{data}. Defaults to the product ID for
ordinary inputs, or the upstream product ID for a nested product.}

\item{execution}{Optional connection-free \code{\link[=dr_execution_config]{dr_execution_config()}} stored on this
definition. Used when it is the root of \code{\link[=dr_run]{dr_run()}}, \code{\link[=dr_publish]{dr_publish()}} or \code{\link[dataraft.lake:dr_ingest]{dataraft.lake::dr_ingest()}}.
An explicit execution argument overrides these defaults.}

\item{contracts}{Named table contracts when data is a dm model.}

\item{table}{Table name to select from a successful model trial or publication.
Published selections retain the exact member release.}
}
\value{
A \code{product}, ready for composition, inspection and execution.
}
\description{
Define a data delivery by name, its expected columns and its quality rules.
A product is a reusable specification. Bind a source with \code{\link[=dr_add_source]{dr_add_source()}},
or supply data when executing a workflow. Use \code{\link[=dr_run]{dr_run()}} before writing.
}
\details{
Add named sources, ordinary transformation functions and optional checks or
a target. Use \code{\link[=dr_run]{dr_run()}} to try it, or \code{\link[=dr_publish]{dr_publish()}} to save checked output.
\code{\link[=dr_run]{dr_run()}} executes the full configuration, including writers. Products can be
sources of other products; shared dependencies run once per execution.
}
\section{Table models}{

A table input defines a table product. A dm input defines a model product:
its tables and declared relationships are checked together and published in
one transaction. dr_collect() returns a dm; select a member explicitly with
dr_product("report", result, table = "policies"). Model execution materializes
all tables in memory. Table contracts are supplied through contracts.
}

\examples{
orders <- dr_product("orders", data.frame(id = 1:2, amount = c(25, 75))) |>
  dplyr::mutate(amount = round(amount, 2)) |>
  dr_add_quality(~ amount >= 0)
orders |> dr_run(write = FALSE, stop_on_failure = FALSE) |> dr_collect()
}
