% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/quality-reports.R
\name{dr_pointblank_report}
\alias{dr_pointblank_report}
\title{Export the native pointblank report from a validation}
\usage{
dr_pointblank_report(x, rule, path, overwrite = FALSE)
}
\arguments{
\item{x}{A quality tibble containing retained pointblank agents.}

\item{rule}{Name supplied to \code{\link[=dr_pointblank_checks]{dr_pointblank_checks()}}.}

\item{path}{Output HTML path.}

\item{overwrite}{Replace an existing output.}
}
\value{
The normalized output path, invisibly.
}
\description{
Requires \code{\link[=dr_validate]{dr_validate()}} with \code{keep_agents = TRUE}. Agents are held only in
memory and are never serialized into the registry. Failed-row extracts and
checked-table samples are disabled during interrogation. The native report
describes pointblank action levels; the authoritative dataraft gate is shown
by \code{\link[=dr_quality_report]{dr_quality_report()}}, especially when using \code{policy = "rule"}.
}
\examples{
\dontshow{if (requireNamespace("pointblank", quietly = TRUE)) withAutoprint(\{ # examplesIf}
contract <- dr_contract("orders", version = "1", owner = "Analytics", description = "Orders", grain = "One order",
  columns = c(amount = "numeric"), rules = list(dr_pointblank_checks("amounts", function(data) {
    pointblank::create_agent(data) |>
      pointblank::col_vals_gte("amount", 0)
  })))
quality <- dr_validate(data.frame(amount = c(10, -1)), contract,
  keep_agents = TRUE)
path <- tempfile(fileext = ".html")
dr_pointblank_report(quality, "amounts", path)
unlink(path)
\dontshow{\}) # examplesIf}
}
