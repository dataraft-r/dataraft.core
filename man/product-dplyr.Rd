% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dplyr-product.R
\name{product-dplyr}
\alias{product-dplyr}
\alias{mutate.dr_product}
\alias{filter.dr_product}
\alias{select.dr_product}
\alias{rename.dr_product}
\alias{relocate.dr_product}
\alias{arrange.dr_product}
\alias{group_by.dr_product}
\alias{ungroup.dr_product}
\alias{summarise.dr_product}
\alias{distinct.dr_product}
\alias{count.dr_product}
\title{Use dplyr verbs in a product definition}
\usage{
\method{mutate}{dr_product}(.data, ...)

\method{filter}{dr_product}(.data, ..., .by = NULL, .preserve = FALSE)

\method{select}{dr_product}(.data, ...)

\method{rename}{dr_product}(.data, ...)

\method{relocate}{dr_product}(.data, ..., .before = NULL, .after = NULL)

\method{arrange}{dr_product}(.data, ..., .by_group = FALSE)

\method{group_by}{dr_product}(.data, ..., .add = FALSE, .drop = dplyr::group_by_drop_default(.data))

\method{ungroup}{dr_product}(x, ...)

\method{summarise}{dr_product}(.data, ..., .by = NULL, .groups = NULL)

\method{distinct}{dr_product}(.data, ..., .keep_all = FALSE)

\method{count}{dr_product}(x, ..., wt = NULL, sort = FALSE, name = NULL)
}
\arguments{
\item{.data, x}{Product definition.}

\item{...}{Arguments captured and passed to the corresponding dplyr verb.}

\item{.by, .preserve, .by_group, .add, .drop, .groups, .keep_all, .before, .after, wt, sort, name}{Arguments with their usual dplyr meanings, evaluated during execution.}
}
\value{
An updated product definition.
}
\description{
Ordinary dplyr verbs append deferred transformations to a product. They use
the same data masking, tidy selection and grouping rules as dplyr on the
acquired table. Nothing is read until \code{\link[=dr_run]{dr_run()}} or \code{\link[=dr_publish]{dr_publish()}}.
}
\details{
Expressions retain their R environments. Inspection records expressions,
plus non-disclosing hashes of referenced lexical bindings. Known input columns
take precedence over ambient names; use \code{.env$name} for explicit lexical
bindings and \code{.data$name} for columns. Changing an external binding can change
a later run: use an explicit \code{code_version} when enabling lake caching, and
an explicit targets cue for dynamic environment or external state. To freeze
a small value in an expression, inject it with \verb{!!}.

Use \code{\link[=dr_step_transform]{dr_step_transform()}} for other functions. Multiple primary sources must
first be combined by a transformation; \code{\link[=dr_step_lookup]{dr_step_lookup()}} adds a checked
auxiliary source without changing the primary table.
\code{left_join()} on a product gives guidance rather than guessing relationship
rules. Use \code{dr_add_lookup(reference, by = ..., name = "reference")} for checked
enrichment, or join ordinary tables inside \code{dr_add_transform()} for other joins.
}
\examples{
orders <- dr_product("orders", data.frame(group = c("a", "a"), amount = c(10, 20))) |>
  dplyr::mutate(tax = amount * 0.2) |>
  dplyr::summarise(total = sum(amount), .by = group)
orders |> dr_run() |> dr_collect()
}
