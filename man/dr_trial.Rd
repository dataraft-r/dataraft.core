% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/trial.R
\name{dr_trial}
\alias{dr_trial}
\title{Try a product without invoking configured writers}
\usage{
dr_trial(x, data = NULL, sources = NULL, stop_on_failure = FALSE)
}
\arguments{
\item{x}{Product or modular \code{\link[=dr_workflow]{dr_workflow()}} definition.}

\item{data, sources}{Replacement delivery or named sources, as in \code{\link[=dr_run]{dr_run()}}.}

\item{stop_on_failure}{Defaults to \code{FALSE}: a failed trial returns a result
for \code{\link[=dr_quality_report]{dr_quality_report()}} and \code{\link[=dr_quality_rows]{dr_quality_rows()}}. Use \code{TRUE} to raise an error.
\code{\link[=dr_run]{dr_run()}} and \code{\link[=dr_publish]{dr_publish()}} still raise errors on failure by default.}
}
\value{
An in-memory run result accepted by \code{\link[=dr_collect]{dr_collect()}} and \code{\link[dataraft.metrics:dr_measure]{dataraft.metrics::dr_measure()}}.
}
\description{
Executes the product's sources, transformations and checks, with targets,
catalogs and durable run evidence disabled throughout its dependency graph.
The original definition is unchanged. Source and transformation callbacks are
ordinary user code: their own side effects cannot be prevented by the framework.
Read-only access to existing published inputs is still allowed. Without a
declared contract a trial is \code{unvalidated}, with collectable exploratory data.
A successful trial checks the data, not target permissions, capacity or conflicts.
}
\examples{
orders <- dr_product("orders", data.frame(amount = c(10, 20))) |>
  dr_add_quality(~ amount >= 0)
dr_trial(orders) |> dr_collect()
}
