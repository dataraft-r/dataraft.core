% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/contract-composition.R
\name{dr_contract_policy}
\alias{dr_contract_policy}
\title{Add validation policy to a contract}
\usage{
dr_contract_policy(
  x,
  required = x$required,
  allow_empty = x$allow_empty,
  allow_extra = x$allow_extra,
  max_age_hours = x$max_age_hours,
  constraints = x$constraints
)
}
\arguments{
\item{x}{Contract specification.}

\item{required}{Non-null columns; keys always remain non-null.}

\item{allow_empty, allow_extra}{Whether empty deliveries or extra columns are allowed.}

\item{max_age_hours}{Freshness limit, or NULL for no freshness monitoring.}

\item{constraints}{Named per-column min, max, enum, nullable, timezone or precision checks.}
}
\value{
An updated contract specification.
}
\description{
Keep identity, columns and keys in \code{\link[=dr_contract]{dr_contract()}} and compose policy only
when required. This builds a new definition without I/O. Changing a registered
contract still requires a new version before registration or publication.
}
\examples{
dr_contract("orders", columns = c(id = "integer"), key = "id") |>
  dr_contract_policy(allow_extra = TRUE)
}
