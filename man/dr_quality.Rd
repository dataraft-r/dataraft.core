% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnostics.R
\name{dr_quality}
\alias{dr_quality}
\title{Retrieve quality evidence for a run or release}
\usage{
dr_quality(
  x,
  run_id = NULL,
  asset = NULL,
  release = NULL,
  ...,
  action = "block",
  threshold = 0,
  engine = "native",
  volatile = FALSE
)
}
\arguments{
\item{x}{A one-sided formula to construct a quality rule, or a lake, run result, measurement, measurement set, dbt result or
quality tibble.
Measurement sets resolve their exact input releases using retained
references. Unavailable references return explicit \code{not_checked} evidence.}

\item{run_id}{Run ID when \code{x} is a lake.}

\item{asset}{Asset ID. Required with \code{release}; otherwise selects the latest
attempt. Supply at most one of \code{run_id} and \code{asset}.}

\item{release}{Exact release ID to inspect, together with \code{asset}.}

\item{...}{For a rule, optional name, description and dimension.}

\item{action, threshold, engine, volatile}{See \code{\link[=dr_quality_rule]{dr_quality_rule()}}.
New definitions use only action and threshold as publication policy.}
}
\value{
A quality tibble. \code{failure_rate} is derived from available counts.
Native pointblank thresholds are recorded in the JSON \code{details} column.
}
\description{
An asset selects its latest attempt, while \code{release} selects the evidence
for that exact published stand. Cached runs resolve to the original release's
checks. These choices prevent an older successful run hiding a recent
failure.
}
\examples{
contract <- dr_contract("orders", version = "1",
  columns = c(id = "integer"), key = "id")
dr_quality(dr_validate(data.frame(id = c(1L, 1L)), contract))
}
\seealso{
\code{\link[=dr_quality_report]{dr_quality_report()}}, \code{\link[=dr_status]{dr_status()}}
}
