% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/execution.R
\name{dr_publish}
\alias{dr_publish}
\alias{dr_publish.data.frame}
\alias{dr_publish.dr_product}
\title{Execute and publish a product with sensible local defaults}
\usage{
dr_publish(x, name = NULL, to = NULL, ...)

\method{dr_publish}{data.frame}(x, name = NULL, to = NULL, ...)

\method{dr_publish}{dr_product}(x, name = NULL, to = NULL, layer = NULL, execution = NULL, ...)
}
\arguments{
\item{x}{Composed product, data frame, or successful dbt build.}

\item{name}{Product name for a data frame, or an exact or unambiguous model
name for a dbt build. Omit it for an already named product.}

\item{to}{Optional target, replacing an already configured target. Defaults
to the \code{dataraft} folder in the working directory when the product has none.
For dbt builds it defaults to the project's configured lake.}

\item{...}{Execution options, including \code{data} or \code{sources} for new deliveries
as described in \code{\link[=dr_run]{dr_run()}}, \code{stop_on_failure} and, for lake
targets, \code{business_date}, \code{notify}, \code{cache} and \code{previous}. Supply a previous
publication to reject stale corrections if the destination has changed. dbt builds accept
\code{\link[dataraft.dbt:dr_dbt_publish]{dataraft.dbt::dr_dbt_publish()}} options such as \code{contract}, \code{asset} and \code{layer}.}

\item{layer}{Optional publication layer for a lake target.}

\item{execution}{Optional \code{\link[=dr_execution_config]{dr_execution_config()}} defaults for products,
overriding defaults stored by \code{dr_product(execution = )}.}
}
\value{
A run result. An exception on failure includes \code{condition$result}.
}
\description{
Use \code{\link[=dr_run]{dr_run()}} to try a product without configured framework writers.
\code{dr_run()} executes its full configuration, including targets and catalogs.
\code{dr_publish()} adds a local lake target when none was supplied. A new folder
uses DuckDB; an existing folder retains its saved backend. \code{\link[dataraft.lake:dr_target_lake]{dataraft.lake::dr_target_lake()}}
accepts other lake configurations. Execution automatically validates the
definition before source acquisition.
Use \code{\link[=dr_collect]{dr_collect()}} to obtain the output as an ordinary tibble.
}
\examples{
\dontshow{if (requireNamespace("dataraft.lake", quietly = TRUE) && requireNamespace("duckdb", quietly = TRUE)) withAutoprint(\{ # examplesIf}
root <- tempfile("dataraft-")
result <- dr_product("orders") |>
  dr_add_source(data.frame(id = 1:2)) |>
  dr_publish(to = root)
dr_collect(result)
unlink(root, recursive = TRUE)
\dontshow{\}) # examplesIf}
}
