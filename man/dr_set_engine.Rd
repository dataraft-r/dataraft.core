% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/set-engine.R
\name{dr_set_engine}
\alias{dr_set_engine}
\title{Choose an implementation without changing a specification}
\usage{
dr_set_engine(x, engine, ...)
}
\arguments{
\item{x}{A \code{\link[=dr_quality_rule]{dr_quality_rule()}} or \code{\link[=dr_lookup_spec]{dr_lookup_spec()}} specification.}

\item{engine}{Supported engine name.}

\item{...}{Reserved for extension methods. Built-in methods reject extras.}
}
\value{
An updated specification. The original is unchanged.
}
\description{
Mirrors parsnip's separation of intent and implementation. Formula quality
rules support \code{"native"} and \code{"pointblank"}; checked lookup specifications
use dm. Engine selection never reads data or loads an
optional engine. Missing dependencies are reported at execution preflight.
These engines implement individual operations, not the whole data platform.
Storage is configured separately with \code{\link[=dr_set_target]{dr_set_target()}} or \code{dr_publish(to = )}.
}
\examples{
positive <- dr_quality_rule("positive", ~ amount > 0)
positive |> dr_set_engine("pointblank")
}
