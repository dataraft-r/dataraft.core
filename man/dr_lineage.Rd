% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnostics.R
\name{dr_lineage}
\alias{dr_lineage}
\title{Follow declared dataset lineage}
\usage{
dr_lineage(
  x,
  asset = NULL,
  direction = c("upstream", "downstream"),
  recursive = TRUE
)
}
\arguments{
\item{x}{Connected lake, run result, measurement, measurement set, dbt result or dbt
artifact directory. Measurement edges describe the input release and
metric version; they have no execution run ID.}

\item{asset}{Optional starting dataset ID (dbt unique ID for dbt inputs).}

\item{direction}{Follow upstream inputs or downstream consumers.}

\item{recursive}{Follow all reachable edges, with cycle protection.}
}
\value{
A tibble of lineage edges with endpoint IDs and versions. dbt edges
have empty version fields because a manifest declares dependencies.
}
\description{
Traverses dataset IDs while retaining version IDs on each returned edge.
It does not infer column lineage or reconstruct a single historical DAG.
Run results expose only recorded inputs for that exact execution, without
querying the latest lake state or inferring unrecorded upstream edges.
}
\examples{
path <- system.file("extdata", "dbt-artifacts", package = "dataraft.dbt")
dr_lineage(path, "model.shop.customer_revenue")
}
