% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/review.R
\name{dr_review}
\alias{dr_review}
\title{Review diagnostics in the IDE data viewer}
\usage{
dr_review(
  result = dr_last_failure(),
  what = c("rows", "report", "lineage"),
  rule = NULL,
  contract = NULL,
  limit = 100
)
}
\arguments{
\item{result}{A diagnostic input accepted by the selected function. Rows
accept run/model results or a table with \code{rule} and, for named checks,
\code{contract}. Reports also accept quality tables, run/dbt results and
measurements. Lineage accepts the inputs documented by \code{\link[=dr_lineage]{dr_lineage()}}.
Omit this argument to review \code{\link[=dr_last_failure]{dr_last_failure()}}.}

\item{what}{Diagnostic table to display: affected rows, quality report or
recorded dataset lineage.}

\item{rule, contract}{Arguments to \code{\link[=dr_quality_rows]{dr_quality_rows()}}. Only valid for rows.
Use \code{"table/check"} to choose a check from a model result.}

\item{limit}{Maximum affected rows to collect. Must be a finite non-negative
whole number; defaults to 100. Only valid for rows. To explicitly request
every affected row, call \code{\link[=dr_quality_rows]{dr_quality_rows()}} directly instead.}
}
\value{
The displayed diagnostic tibble, invisibly.
}
\description{
Opens a diagnostic table with \code{\link[utils:View]{utils::View()}} and returns that same table
invisibly. IDEs such as Positron and RStudio display it in their data viewer.
Use the diagnostic functions directly in headless sessions.
}
\details{
\code{"rows"} delegates to \code{\link[=dr_quality_rows]{dr_quality_rows()}} and collects at most \code{limit}
affected rows. Row predicates are re-evaluated against the retained candidate;
keep their captured constants unchanged. \code{"report"} uses \code{\link[=dr_quality_report]{dr_quality_report()}}
without exporting a file. \code{"lineage"} uses \code{\link[=dr_lineage]{dr_lineage()}}. These last two
choices inspect metadata, not the source rows. No run is retried and the
retained last failure is not cleared.
}
\examples{
\dontshow{if (interactive()) withAutoprint(\{ # examplesIf}
orders <- dr_product("orders", data.frame(amount = c(10, -2))) |>
  dr_add_quality(list(positive = ~ amount >= 0))
result <- dr_run(write = FALSE, orders, stop_on_failure = FALSE)
dr_review(result, rule = "positive", limit = 20)
dr_review(result, what = "report")
\dontshow{\}) # examplesIf}
}
