% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/quality-engine.R
\name{dr_run_quality}
\alias{dr_run_quality}
\title{Evaluate an interchangeable quality rule}
\usage{
dr_run_quality(rule, data, ...)
}
\arguments{
\item{rule}{A rule from \code{\link[=dr_quality_rule]{dr_quality_rule()}}, \code{\link[=dr_pointblank_checks]{dr_pointblank_checks()}}, or an extension.}

\item{data}{Data frame or lazy table.}

\item{...}{Adapter-specific options. Native methods accept \code{keep_agent}.}
}
\value{
A quality tibble, with one row per evaluated check.
}
\description{
Extension packages implement an S3 method for their rule class, inheriting
from \code{dr_rule}. Return the same columns as a native call. Unknown states,
empty evidence and malformed results never authorize publication.
Rule exceptions are retained locally only when requested by \code{\link[=dr_validate]{dr_validate()}}.
}
\examples{
dr_run_quality(dr_quality_rule("positive", ~ amount > 0),
  data.frame(amount = c(10, -1, NA)))
}
