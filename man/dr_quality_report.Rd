% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/quality-reports.R
\name{dr_quality_report}
\alias{dr_quality_report}
\title{Inspect or export a portable quality report}
\usage{
dr_quality_report(
  x,
  path = NULL,
  format = c("html", "json"),
  title = "dataraft quality report",
  overwrite = FALSE
)
}
\arguments{
\item{x}{Quality tibble or a run/dbt result accepted by \code{\link[=dr_quality]{dr_quality()}}.}

\item{path}{Optional output file path. Omit it to inspect a compact table.}

\item{format}{HTML or JSON.}

\item{title}{Human-readable report title.}

\item{overwrite}{Replace an existing file only when explicitly requested.}
}
\value{
A diagnostic tibble when \code{path} is omitted; otherwise the normalized
output path, invisibly.
}
\description{
Exports persisted check metadata, counts and segment labels. No source rows
are collected. The HTML is self-contained, escapes cell contents and displays
status as text. An empty result is explicitly reported as not checked.
}
\examples{
contract <- dr_contract("orders", version = "1",
  columns = c(id = "integer"), key = "id")
quality <- dr_validate(data.frame(id = c(1L, 1L)), contract)
path <- tempfile(fileext = ".html")
dr_quality_report(quality, path)
unlink(path)
}
\seealso{
\code{\link[=dr_pointblank_report]{dr_pointblank_report()}}, \code{\link[=dr_expect_quality]{dr_expect_quality()}}
}
