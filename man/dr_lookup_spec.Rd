% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/set-engine.R
\name{dr_lookup_spec}
\alias{dr_lookup_spec}
\title{Specify a reusable checked relationship}
\usage{
dr_lookup_spec(
  source,
  by,
  unmatched = c("error", "keep"),
  suffix = c(".x", ".y"),
  name = NULL,
  table = NULL
)
}
\arguments{
\item{source}{Reference data, path, source adapter, product or successful result.}

\item{by}{Equality keys as a character vector, named vector or \code{\link[dplyr:join_by]{dplyr::join_by()}}.}

\item{unmatched}{Whether unmatched input rows fail or retain missing attributes.}

\item{suffix}{Two suffixes for overlapping non-key column names.}

\item{name}{Stable reference delivery name for source replacement.}

\item{table}{Member table to select from a successful model result.}
}
\value{
A deferred lookup transformation specification.
}
\description{
Defines an enrichment independently of a product or recipe. Use \code{\link[=dr_set_engine]{dr_set_engine()}}
to select dm constraint checks, then attach it with
\code{\link[=dr_step_transform]{dr_step_transform()}}.
}
\examples{
customers <- data.frame(id = 1:2, region = c("North", "South"))
lookup <- dr_lookup_spec(customers, by = "id", name = "customers") |>
  dr_set_engine("dm")
dr_recipe() |> dr_step_transform(lookup)
}
