% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/execution.R
\name{dr_execute_target}
\alias{dr_execute_target}
\title{Target execution protocol}
\usage{
dr_execute_target(target, product, ...)
}
\arguments{
\item{target}{Target adapter, or \code{NULL} for in-memory execution.}

\item{product}{Validated product specification.}

\item{...}{Execution options. Target adapters must reject unsupported options.}
}
\value{
A run result containing execution evidence and an output reference.
}
\description{
Execute a product using a target adapter
}
\details{
Most extensions only need \code{\link[=dr_write_target]{dr_write_target()}}: the default executor reads the
source, applies transforms, checks the candidate, then calls the writer.
Implement this generic only when execution needs different resource or
transaction semantics. The lake adapter compiles the product into the
existing governed ingestion lifecycle. Methods return \code{dr_run_result}.
}
\examples{
product <- dr_product("orders") |> dr_add_source(data.frame(id = 1:2))
dataraft.core::dr_execute_target(NULL, dr_validate(product))
}
\keyword{internal}
