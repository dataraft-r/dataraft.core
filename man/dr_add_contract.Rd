% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/composition.R
\name{dr_add_contract}
\alias{dr_add_contract}
\alias{dr_add_quality}
\title{Attach output requirements to a product specification}
\usage{
dr_add_contract(x, contract)

dr_add_quality(
  x,
  quality,
  name = NULL,
  engine = NULL,
  action = NULL,
  threshold = NULL,
  dimension = NULL
)
}
\arguments{
\item{x}{A \code{\link[=dr_product]{dr_product()}} specification or modular \code{\link[=dr_workflow]{dr_workflow()}}.}

\item{contract}{Contract, named type vector, or named list of prototypes.}

\item{quality}{One-sided row predicate, function, rule, or list of rules.
Formula \code{NA} results count as failures. Functions return scalar logicals
or \code{\link[=dr_quality_counts]{dr_quality_counts()}}. Names in rule lists become rule names.}

\item{name}{Optional quality rule name.}

\item{engine}{Optional formula quality engine, \code{"native"} or
\code{"pointblank"}. Omit to preserve engines on existing rule specifications.}

\item{action}{Optional publication policy: block, warn or quarantine. Quarantine
removes every failing row before writing and retains it locally on the result.
It supports native row formulas only. Standalone validation still blocks
rejected rows until an execution path actually removes them.}

\item{threshold}{Permitted fraction of failed test units, from zero to one. For
quarantine, every rejected row is removed regardless of threshold.}

\item{dimension}{Optional ODCS quality dimension.}
}
\value{
An updated product specification, without executing checks.
}
\description{
The contract describes shape and keys; quality rules describe acceptable
values. Checks apply after preparation and gate framework writers.
Adding a contract replaces the previous contract; \code{NULL} removes it.
Quality rules accumulate.
}
\examples{
dr_product("orders") |>
  dr_add_contract(c(id = "integer", amount = "numeric")) |>
  dr_add_quality(~ amount >= 0)
}
\seealso{
\code{\link[=dr_contract]{dr_contract()}}, \code{\link[=dr_quality_rule]{dr_quality_rule()}}, \code{\link[=dr_set_engine]{dr_set_engine()}}
}
