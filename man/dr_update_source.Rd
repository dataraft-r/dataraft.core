% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/component-editing.R
\name{dr_update_source}
\alias{dr_update_source}
\alias{dr_extract_source}
\alias{dr_remove_source}
\title{Edit named primary sources without reading them}
\usage{
dr_update_source(x, source, name = NULL, reader = NULL)

dr_extract_source(x, name = NULL)

dr_remove_source(x, name = NULL)
}
\arguments{
\item{x}{A product or modular workflow definition.}

\item{source}{Replacement data frame, path, function or source adapter.}

\item{name}{Existing primary source name, or NULL for the only source.}

\item{reader}{Optional file reader, as in \code{\link[=dr_add_source]{dr_add_source()}}.}
}
\value{
An updated definition, or the extracted source specification.
}
\description{
Updating requires an existing source. Extraction returns its stored adapter,
function or data frame, never its executed result. If \code{name} is omitted,
exactly one source must exist. Removing an explicitly named absent source is
idempotent. Only sources owned by \code{x} are edited: extract the product first to
edit sources attached to a workflow's product. Lookup dependencies remain in
their recipe steps and are not primary source slots.
}
\examples{
product <- dr_product("orders") |>
  dr_add_source(data.frame(id = 1L), name = "delivery")
dr_update_source(product, data.frame(id = 2L)) |> dr_extract_source()
dr_remove_source(product, "delivery")
}
