% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/products.R
\name{dr_model}
\alias{dr_model}
\title{Create and validate a relational dm model from pinned releases}
\usage{
dr_model(
  lake,
  tables,
  primary_keys = list(),
  foreign_keys = list(),
  releases = NULL,
  check = TRUE
)
}
\arguments{
\item{lake}{Connected lake.}

\item{tables}{Named character vector of asset ids.}

\item{primary_keys}{Named list of character vectors, keyed by table alias.}

\item{foreign_keys}{List of lists containing table, columns, ref_table,
ref_columns.}

\item{releases}{Optional named vector pinning all table releases.}

\item{check}{Validate all declared keys and relationships.}
}
\value{
A dm object containing lazy tables. No automatic flattening is done.
}
\description{
Create and validate a relational dm model from pinned releases
}
\examples{
\dontshow{if (requireNamespace("dataraft.lake", quietly = TRUE) && requireNamespace("duckdb", quietly = TRUE) && requireNamespace("dm", quietly = TRUE)) withAutoprint(\{ # examplesIf}
root <- tempfile("dataraft-example-")
config <- dataraft.lake::dr_lake_config(
  dataraft.lake::dr_registry_duckdb(file.path(root, "lake.db")),
  dataraft.lake::dr_storage_local(file.path(root, "data")),
  landing = file.path(root, "landing"), backend = "duckdb"
)
lake <- dataraft.lake::dr_connect_lake(config)
path <- file.path(root, "orders.csv")
utils::write.csv(data.frame(order_id = 1:2, amount = c(25, 75)), path,
  row.names = FALSE)
source <- dr_source_file("orders.file", path, reader = utils::read.csv)
contract <- dr_contract(
  "orders", version = "1.0.0", columns =
  c(order_id = "integer", amount = "numeric"), key = "order_id"
)
release <- dr_product("orders", contract = contract, code_version = "v1") |>
  dr_add_source(source) |> dr_publish(to = lake)
model <- dr_model(lake, c(orders = "orders"),
  primary_keys = list(orders = "order_id"))
model
dataraft.lake::dr_disconnect_lake(lake)
unlink(root, recursive = TRUE)
\dontshow{\}) # examplesIf}
}
