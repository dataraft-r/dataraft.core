% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnostics.R
\name{dr_status}
\alias{dr_status}
\title{Inspect execution status across R and dbt workflows}
\usage{
dr_status(x, asset = NULL)
}
\arguments{
\item{x}{A connected lake, \code{\link[=dr_run]{dr_run()}} result, \code{\link[dataraft.dbt:dr_dbt_build]{dataraft.dbt::dr_dbt_build()}} result or a
measurement or measurement set from \code{\link[dataraft.metrics:dr_measure]{dataraft.metrics::dr_measure()}}, or a \code{\link[=dr_workflow]{dr_workflow()}} result.}

\item{asset}{Optional asset ID when querying a lake.}
}
\value{
A tibble with \code{engine}, \code{id}, \code{status}, \code{success}, \code{release_id},
\code{asset}, \code{outcome} and \code{message}. \code{outcome} normalizes native statuses to
\code{succeeded}, \code{blocked}, \code{failed} or \code{skipped} (unknown states are \code{NA}).
Missing deliveries are \code{blocked} because no acceptable input is available.
A dbt process failure remains visible even when
individual nodes passed. No raw stdout or stderr is included. Workflow
results return a compact table with \code{step}, \code{status} and \code{success}.
}
\description{
Inspect execution status across R and dbt workflows
}
\examples{
\dontshow{if (requireNamespace("duckdb", quietly = TRUE)) (if (getRversion() >= "3.4") withAutoprint else force)(\{ # examplesIf}
root <- tempfile("dataraft-")
lake <- dataraft.lake::dr_connect_lake(dataraft.lake::dr_lake_config(dataraft.lake::dr_registry_duckdb(file.path(root, "lake.db")),
  dataraft.lake::dr_storage_local(file.path(root, "data")),
  landing = file.path(root, "landing"), backend = "duckdb"))
dr_status(lake)
dataraft.lake::dr_disconnect_lake(lake)
unlink(root, recursive = TRUE)
\dontshow{\}) # examplesIf}
}
