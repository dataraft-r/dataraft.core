% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/composition.R
\name{dr_add_source}
\alias{dr_add_source}
\title{Bind a source to a product or workflow}
\usage{
dr_add_source(x, source, name = NULL, reader = NULL, replace = FALSE)
}
\arguments{
\item{x}{A \code{\link[=dr_product]{dr_product()}} or modular \code{\link[=dr_workflow]{dr_workflow()}} definition.}

\item{source}{Data frame, file path, function or source adapter.}

\item{name}{Optional source name, generated when omitted.}

\item{reader}{Optional file reader. CSV, TSV, RDS and Excel have defaults.}

\item{replace}{Replace a source with the same name explicitly.}
}
\value{
An updated definition. No source data are read.
}
\description{
Store a named primary input without reading it. A workflow can instead bind
one delivery with \verb{data =} at execution. For several primary sources, use a
transformation that combines their named list before table operations.
}
\examples{
flow <- dr_workflow() |>
  dr_add_product(dr_product("orders")) |>
  dr_add_source(data.frame(id = 1:2, amount = c(25, 75)), name = "orders")
dr_collect(dr_run(write = FALSE, stop_on_failure = FALSE, flow))
}
\seealso{
\code{\link[=dr_set_sources]{dr_set_sources()}}, \code{\link[dataraft.adapters:dr_source_database]{dataraft.adapters::dr_source_database()}}, \code{\link[=dr_run]{dr_run()}}
}
