% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/backend-protocols.R
\name{backend-protocols}
\alias{dr_as_target}
\alias{dr_as_source}
\alias{dr_land_source}
\alias{dr_output_source}
\alias{dr_read_output}
\alias{dr_read_diagnostic_rows}
\alias{dr_publish_model_result}
\alias{dr_acquire_write_session}
\alias{dr_set_target_layer}
\title{Backend extension protocols}
\usage{
dr_as_target(x, ...) UseMethod("dr_as_target")
dr_as_source(x, ...) UseMethod("dr_as_source")
dr_land_source(x, source, ...) UseMethod("dr_land_source")
dr_output_source(x, asset, release, ...) UseMethod("dr_output_source")
dr_read_output(x, asset, release, ...) UseMethod("dr_read_output")
dr_read_diagnostic_rows(x, diagnostic, rule = NULL, limit = 100, ...) UseMethod("dr_read_diagnostic_rows")
dr_publish_model_result(x, product, result, previous = NULL, ...) UseMethod("dr_publish_model_result")
dr_acquire_write_session(x, scope = parent.frame(), ...) UseMethod("dr_acquire_write_session")
dr_set_target_layer(x, layer, ...) UseMethod("dr_set_target_layer")
}
\arguments{
\item{x}{Backend provider, destination or target adapter.}
\item{source}{File source to archive.}
\item{asset,release}{Exact asset and release identifiers.}
\item{diagnostic}{Retained diagnostic descriptor.}
\item{rule}{Rule to diagnose.}
\item{limit}{Maximum diagnostic rows.}
\item{product,result}{Checked product definition and model result.}
\item{previous}{Previous publication for optimistic concurrency.}
\item{scope}{Environment owning the write-session lifetime.}
\item{layer}{Publication layer.}
\item{...}{Adapter-specific options.}
}
\value{Adapter-specific values described by each operation.}
\description{Storage adapters implement these public S3 protocols. Unsupported operations fail explicitly. Source and target coercion perform no I/O. Output readers use exact release IDs. Diagnostic readers collect bounded rows before closing owned connections. Write sessions coordinate with maintenance until their explicit scope exits. Load the provider package before using shorthand paths or serialized provider objects. Core does not discover sibling packages by name; the metapackage loads its providers, and provider constructors load their namespaces.}
