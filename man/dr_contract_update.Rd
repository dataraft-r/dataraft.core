% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/contract-update.R
\name{dr_contract_update}
\alias{dr_contract_update}
\title{Reuse and explicitly revise a contract}
\usage{
dr_contract_update(
  x,
  id = x$id,
  version = x$version,
  columns = NULL,
  remove = character(),
  ...
)
}
\arguments{
\item{x}{A confirmed contract specification.}

\item{id, version}{Identifier and definition version. At least one must differ
from \code{x}; version ordering is not inferred.}

\item{columns}{Named type vector or zero-length prototypes to add or replace,
as in \code{\link[=dr_contract]{dr_contract()}}. Omit to retain the declared types.}

\item{remove}{Character vector of declared column names to remove.}

\item{...}{Named contract fields to replace in full, such as
\code{required}, \code{key}, \code{grain}, \code{rules}, \code{owner} or \code{column_metadata}.}
}
\value{
A new \code{contract} specification. Use \code{\link[=dr_contract_diff]{dr_contract_diff()}} to review it.
}
\description{
Adds or replaces declared column types without reading data or executing
rules. New columns are optional unless explicitly included in \code{required}.
Change \code{id} for a derived product, or \code{version} for a revised definition of
the same contract. The original specification is unchanged.
}
\details{
Contracts describe final output expectations. Deferred dplyr transformations
do not rewrite them. After renaming, remove the old column and add the new
one here. After aggregation, explicitly set the new \code{grain} and \code{key}.
A changed grain requires an explicit key, including \code{character()} when no
key is claimed. Removing or changing a key column also requires an explicit
key. Removing a required column requires an explicit \code{required} vector.

Existing rules must be explicitly replaced or reaffirmed with \code{rules} after
a removal, type change or grain change. Rules may contain arbitrary R code,
so their dependencies cannot safely be inferred. This review prevents silent
inheritance of guarantees, but is not proof that revised rules are correct.
Metadata for removed columns is discarded; other metadata is inherited.
}
\examples{
orders <- dr_contract("orders", columns = c(id = "integer"), key = "id")
enriched <- orders |>
  dr_contract_update(id = "enriched_orders", columns = c(channel = "character"))
dr_contract_diff(orders, enriched)
}
\seealso{
\code{\link[=dr_contract]{dr_contract()}}, \code{\link[=dr_contract_diff]{dr_contract_diff()}}
}
