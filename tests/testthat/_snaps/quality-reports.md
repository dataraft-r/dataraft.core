# reports escape metadata, expose the gate and never include retained agents

    Code
      dr_quality_report(quality, html)
    Condition
      Error in `dr_quality_report()`:
      ! Output exists; choose another path or overwrite = TRUE.

